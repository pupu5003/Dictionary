{
    "tasks": [
        {
            "type": "shell",
            "label": "Build OSX",
            "command": "make",
            "options": { "cwd": "${workspaceFolder}" }
            }
    ],
    "version": "2.0.0"
}