// #include <raylib.h>
// #include <cwchar>

// RLAPI void DrawTextWChar(const wchar_t *text, int posX, int posY, int fontSize, Color color);       // Draw text (using default font)
// RLAPI int MeasureTextWChar(const wchar_t *text, int fontSize);                                      // Measure string width for default font
// RLAPI void DrawTextExWChar(Font font, const wchar_t *text, Vector2 position, float fontSize, float spacing, Color tint); // Draw text using font and additional parameters
